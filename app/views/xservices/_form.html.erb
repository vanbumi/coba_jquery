<%= form_for(@xservice) do |f| %>
  <% if @xservice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@xservice.errors.count, "error") %> prohibited this xservice from being saved:</h2>

      <ul>
      <% @xservice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
